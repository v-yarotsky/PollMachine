%section
  %h1= poll.title
  - if poll.description?
    %p.description= poll.description
  %h3= pluralize(poll.questions.size, "question")
  = link_to "Proceed", show_questions_poll_path(poll), :class => "button proceed", :id => "proceed_to_questions"