%section
  %h1= poll.title
  - if poll.description?
    %p.description= poll.description
  %h3= pluralize(poll.questions.size, "question")
  = link_to "Proceed", new_poll_answers_path(poll), :class => "button proceed", :id => "proceed_to_questions"