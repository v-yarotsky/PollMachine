%section
  %h1= @poll.title
  - if @poll.description?
    %p.description= @poll.description
  %h3= pluralize(@poll.questions.size, "question")
  = link_to "Proceed", poll_questions_path(@poll), :class => "button", :id => "proceed_to_questions"