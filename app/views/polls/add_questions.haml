= nested_form_for poll, :html => { :id => "poll_questions" } do |f|
  = f.fields_for :questions do |q|
    = render "question_fields", :f => q
  = f.link_to_add "Add question", :questions
  = f.submit